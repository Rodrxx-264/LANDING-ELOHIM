'use client';
import {useState} from 'react'; import {LocateFixed,Navigation,LoaderCircle,MessageCircle} from 'lucide-react'; import {casasDePaz,whatsappUrl} from '@/data/casasDePaz';
function distanceKm(a:number,b:number,c:number,d:number){const R=6371,x=(c-a)*Math.PI/180,y=(d-b)*Math.PI/180,z=Math.sin(x/2)**2+Math.cos(a*Math.PI/180)*Math.cos(c*Math.PI/180)*Math.sin(y/2)**2;return R*2*Math.atan2(Math.sqrt(z),Math.sqrt(1-z))}
export default function NearestCasa(){const[loading,setLoading]=useState(false),[result,setResult]=useState<any>(null),[error,setError]=useState('');function locate(){setError('');if(!navigator.geolocation){setError('Tu navegador no permite obtener la ubicación.');return}setLoading(true);navigator.geolocation.getCurrentPosition(p=>{const{latitude,longitude}=p.coords;const r=casasDePaz.map(c=>({...c,distance:distanceKm(latitude,longitude,c.lat,c.lng)})).sort((a,b)=>a.distance-b.distance)[0];setResult(r);setLoading(false)},()=>{setLoading(false);setError('No pudimos acceder a tu ubicación. Puedes elegir una Casa de Paz abajo.')},{enableHighAccuracy:false,timeout:10000,maximumAge:60000})}return <section className="px-4 py-10 md:px-8 md:py-20"><div className="mx-auto max-w-5xl overflow-hidden rounded-[32px] border border-[var(--accent)]/20 bg-[radial-gradient(circle_at_80%_20%,rgba(244,184,74,.15),transparent_26rem)] p-6 md:p-10"><div className="grid gap-8 md:grid-cols-[1fr_auto] md:items-center"><div><div className="mb-4 flex items-center gap-2 text-xs font-bold tracking-[.22em] text-[var(--accent)]"><span className="pulse-dot h-2 w-2 rounded-full bg-[var(--accent)]"/> SOLO EN TU DISPOSITIVO</div><h2 className="font-display text-3xl font-bold md:text-5xl">Encuentra la Casa más cercana.</h2><p className="mt-3 max-w-xl text-sm leading-6 text-[var(--muted)]">Permite tu ubicación y calcularemos cuál de las tres está más cerca. No guardamos tu ubicación.</p></div>{!result?<button onClick={locate} disabled={loading} className="inline-flex min-h-14 items-center justify-center gap-2 rounded-full bg-[var(--accent)] px-6 font-bold text-[#17120a] disabled:opacity-60">{loading?<LoaderCircle className="animate-spin" size={20}/>:<LocateFixed size={20}/>} {loading?'Buscando…':'Permitir ubicación'}</button>:<div className="min-w-[260px] rounded-3xl border border-white/10 bg-black/20 p-5"><p className="text-xs font-bold tracking-[.18em] text-[var(--accent)]">TU CASA MÁS CERCANA</p><p className="mt-2 font-display text-2xl font-bold">{result.nombre}</p><p className="mt-1 text-sm text-[var(--muted)]">{result.distance.toFixed(1)} km de distancia</p><div className="mt-4 flex gap-2"><a href={result.mapsUrl} target="_blank" rel="noreferrer" className="inline-flex flex-1 items-center justify-center gap-2 rounded-full bg-white px-4 py-3 text-xs font-bold text-black"><Navigation size={15}/> Cómo llegar</a><a href={whatsappUrl(result.whatsapp)} target="_blank" rel="noreferrer" className="grid h-11 w-11 place-items-center rounded-full border border-white/15"><MessageCircle size={17}/></a></div></div>}</div>{error&&<p className="mt-5 text-sm text-amber-200">{error}</p>}</div></section>}
