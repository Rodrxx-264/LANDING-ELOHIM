'use client'; import {Home,MapPin,Map,Camera} from 'lucide-react'; const items=[['Inicio','#inicio',Home],['Casas','#casas',MapPin],['Mapa','#mapa',Map],['Fotos','#fotos',Camera]] as const; export default function MobileNavigation(){return <nav className="glass fixed inset-x-3 bottom-3 z-40 grid grid-cols-4 rounded-2xl p-1 md:hidden" aria-label="Navegación principal">{items.map(([l,h,I])=><a key={l} href={h} className="flex min-h-12 flex-col items-center justify-center gap-0.5 rounded-xl text-[10px] font-semibold text-white/60 hover:bg-white/5 hover:text-white"><I size={17}/>{l}</a>)}</nav>}
