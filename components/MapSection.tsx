'use client';
import {useEffect} from 'react'; import {MapContainer,Marker,Popup,TileLayer,useMap} from 'react-leaflet'; import L from 'leaflet'; import 'leaflet/dist/leaflet.css'; import {Navigation} from 'lucide-react'; import {casasDePaz} from '@/data/casasDePaz';
function Fit(){const map=useMap();useEffect(()=>{map.fitBounds(L.latLngBounds(casasDePaz.map(c=>[c.lat,c.lng] as [number,number])),{padding:[42,42]})},[map]);return null}
export default function MapSection(){return <section id="mapa" className="px-4 py-16 md:px-8 md:py-24"><div className="mx-auto max-w-6xl"><div className="mb-7 md:flex md:items-end md:justify-between"><div><p className="mb-3 text-xs font-bold tracking-[.25em] text-[var(--accent)]">UBICACIONES</p><h2 className="font-display text-4xl font-bold tracking-tight md:text-6xl">¿Dónde estamos?</h2></div><p className="mt-4 max-w-md text-sm leading-6 text-[var(--muted)] md:mt-0">Tres puntos en Villa Hermosa. Toca un marcador para ver la información y llegar.</p></div><div className="h-[390px] overflow-hidden rounded-[28px] border border-white/10 shadow-2xl md:h-[520px]"><MapContainer center={[14.5187,-90.5478]} zoom={15} scrollWheelZoom={false}><TileLayer attribution="&copy; OpenStreetMap contributors" url="https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png"/><Fit/>{casasDePaz.map(c=>{const icon=L.divIcon({className:'',html:`<div class="custom-marker">${String(c.id).padStart(2,'0')}</div>`,iconSize:[38,38],iconAnchor:[19,19]});return <Marker key={c.id} position={[c.lat,c.lng]} icon={icon}><Popup><strong>{c.nombre}</strong><div style={{marginTop:6,fontSize:12,opacity:.72}}>{c.horario}</div><a href={c.mapsUrl} target="_blank" rel="noreferrer" style={{display:'inline-flex',gap:6,marginTop:10,fontWeight:700,fontSize:12}}><Navigation size={14}/> Cómo llegar</a></Popup></Marker>})}</MapContainer></div></div></section>}
